using UnityEngine;

public class FlowerScript : MonoBehaviour
{
    public string colorName; // Set in the Inspector to match the flower's color
    public bool isMatched = false; // Track if this flower has been successfully matched
}
